function upload( req, res ){
}

function bucketMeta( req, res ){
}

function modifyBucketMeta( req, res ){
}

function sendFile( req, res ){
}

function sendFileMeta( req, res ){
}

function sendFilePreview( req, res ){
}

function overwriteFile( req, res ){
}

function modifyFileMeta( req, res ){
}

module.exports = {
  upload: upload,
  bucketMeta: bucketMeta,
  modifyBucketMeta: modifyBucketMeta,
  sendFile: sendFile,
  sendFileMeta: sendFileMeta,
  sendFilePreview: sendFilePreview,
  overwriteFile: overwriteFile,
  modifyFileMeta: modifyFileMeta
}
